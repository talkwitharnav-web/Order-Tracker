# Docker export artifacts (generated by export.ps1/export.cmd -- large
# binary output, regenerate anytime by re-running the export command rather
# than committing it).
restaurant-app-export/
restaurant-app-export.zip
